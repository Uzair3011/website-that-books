# HTTPS CRM / automation webhook. The endpoint must accept JSON and return 2xx.
LEAD_WEBHOOK_URL=
# Optional shared secret sent as Authorization: Bearer <token>.
LEAD_WEBHOOK_TOKEN=
# Optional canonical origin, e.g. https://your-real-domain.com
SITE_URL=
PORT=4174
