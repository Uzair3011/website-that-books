// Public contact details only. Keep API keys and webhook URLs in server environment variables.
window.VELTRA_CONFIG = Object.freeze({
  bookingUrl: "", // Google Calendar appointment schedule or Calendly HTTPS URL
  email: "",
  phone: "", // International format, e.g. +country-code followed by number
  whatsapp: "", // International number, digits only
});
